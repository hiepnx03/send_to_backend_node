# Automated Webhook

Gửi ảnh và dữ liệu từ điển đến một URL bên ngoài thông qua yêu cầu POST.

## Đầu vào (Inputs)
- **webhook_url**: URL đích.
- **images**: (Tùy chọn) Nhóm ảnh để tải lên dưới dạng `files`.
- **data**: (Tùy chọn) Dữ liệu DICT để gửi dưới dạng JSON.
